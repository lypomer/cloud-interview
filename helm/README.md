<div align="center">
  <h1>🚢 Helm</h1>
</div>

## Context
This folder contains everything related to [helm](https://helm.sh/) charts required by the project.

## Folder structure
```bash
.
├── charts # custom charts definitions
├── README.md
└── values # charts values overwrites
```

- `charts` contains the custom apps packaging definition used in this project
- `values` contains the values files used to overwrite default charts behaviors